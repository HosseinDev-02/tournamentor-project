import { createSlice } from "@reduxjs/toolkit";

const initialState = []

const typeAvatarsSlice = createSlice({
    name: "typeAvatars",
    initialState,
    reducers: {},
});

export default typeAvatarsSlice.reducer;